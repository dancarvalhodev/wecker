# PHP Modernization Report — `<relative/path/to/File.php>`

> Generated by the `modernize-php` skill. Target PHP version: **8.5**.
> Audience note: explanations assume a junior developer, so each item says *what* to change, *how*, and *why it matters*.

## Summary

<2–4 sentence overview: how modern is the file already, the biggest wins, and roughly how much effort adapting it is (low/medium/high). Mention what the file does so the reader has context.>

| # | Recommendation | Feature (since) | Priority | Effort |
|---|----------------|-----------------|----------|--------|
| 1 | <short title> | <feature — PHP x.y> | High/Med/Low | S/M/L |
| 2 | ... | ... | ... | ... |

Priority = impact on correctness/security/readability. Security and correctness issues are always **High**.

---

## Recommendations

### 1. <Short, imperative title, e.g. "Replace the status `switch` with `match`">

**Where:** `File.php:<line or method name>`

**What it looks like now**
```php
// paste the actual current snippet from the file
```

**Suggested change**
```php
// the modernized version
```

**Why change it**
<Plain-language reason. For a junior dev: explain the underlying concept, not just "it's newer". E.g. what problem the old code can cause, what the new feature guarantees, and any gotcha to watch for.>

**Since:** PHP <x.y>. **Risk of changing:** <none / behavior-preserving / needs a test because ...>

---

### 2. ...

(repeat the block for each recommendation, ordered by priority)

---

## Not changed on purpose

<Optional: list things a reader might expect to be flagged but that are fine as-is, and why. Prevents "why didn't it mention X" confusion. Also note anything you were unsure about rather than guessing.>

## Learn more

- Pipe operator, `clone with`, `#[\NoDiscard]` and other 8.5 additions: PHP 8.5 release notes.
- Only cite features that actually apply to this file.
